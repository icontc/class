### **HTML এর মৌলিক কাঠামো (Basic Structure of HTML) বাংলা ভাষায়**  

HTML (HyperText Markup Language) হলো ওয়েব পেজ তৈরির জন্য ব্যবহৃত একটি ভাষা। প্রতিটি HTML ডকুমেন্টের একটি নির্দিষ্ট কাঠামো থাকে।  

---

### **HTML এর মৌলিক কাঠামো**  
একটি সাধারণ HTML পেজ নিম্নলিখিত কাঠামো অনুসারে তৈরি করা হয়ঃ  

```html
<!DOCTYPE html>
<html lang="bn">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>আমার প্রথম ওয়েব পেজ</title>
</head>
<body>
    <h1>স্বাগতম!</h1>
    <p>এটি একটি সাধারণ HTML ওয়েব পেজের উদাহরণ।</p>
</body>
</html>
```

---

### **প্রধান অংশসমূহের ব্যাখ্যা**  

1. `<!DOCTYPE html>` → এটি HTML5 ডকুমেন্ট নির্দেশ করে।  
2. `<html>` → HTML ডকুমেন্টের মূল ট্যাগ।  
3. `<head>` → এখানে পেজের মেটা তথ্য, শিরোনাম এবং স্টাইল সংরক্ষিত থাকে।  
4. `<meta charset="UTF-8">` → বাংলা এবং অন্যান্য ভাষা সমর্থনের জন্য ব্যবহৃত হয়।  
5. `<title>` → ব্রাউজারের ট্যাবে দেখানো টাইটেল।  
6. `<body>` → এখানে মূল কন্টেন্ট (যেমনঃ টেক্সট, ছবি, লিংক ইত্যাদি) থাকে।  
7. `<h1>` → এটি একটি শিরোনাম ট্যাগ (হেডিং ১ - সবচেয়ে বড় শিরোনাম)।  
8. `<p>` → এটি অনুচ্ছেদ (paragraph) তৈরি করে।  

---

### **উদাহরণ রেজাল্ট (Output)**  
আপনি যদি উপরের HTML কোডটি ব্রাউজারে রান করেন, তাহলে নিচের মত আউটপুট দেখাবে:

#### **স্বাগতম!**  
এটি একটি সাধারণ HTML ওয়েব পেজের উদাহরণ।  

এইভাবে, HTML এর মৌলিক কাঠামো তৈরি করা হয়। 😊
